const project = (name, height, country) => {
    console.log(`Name: ${name}`);
    console.log(`Height: ${height}`);
    console.log(`Country: ${country}`);
};

project("Adeoluwa", 5.10, "Nigeria");

// Using functions make the code reuasble for any number of cases.